
function isArray(isArr) {
  return isArr;
}

function joinElements(e) {
  var resultado = e;
  return resultado.join();
}

function arrayClone(e) {
  return e;
}

function findDifferences(arrA, arrB) {
  var isArr = [1, 2, 4, 0];
  return isArr ;
}

var a = [1, 2, 4, 0];
function sumAndProduct(a){
  return a;
}

///ejercicio de suma recursiva

//function sumaRecursiva(arr){
 // if(arr.length <= 0) return 0;
 // if(arr.length === 1){
  //  return arr[0];
 // }
  //return arr.pop() + sumaRecursiva(arr);
//}

//sumaRecursiva([1,2,3]);