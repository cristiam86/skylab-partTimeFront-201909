
function isArray(arr) {
    return Array.isArray(arr);
}

function joinElements(input){
    var result = input.join("")
    var result2 = result.replace(" ","")
    return result2
}

function arrayClone(input){
    var cloneInput = input;
    var distraction = input;
}

